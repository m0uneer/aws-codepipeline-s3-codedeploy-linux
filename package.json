{
  "name": "aws-codepipeline-s3-codedeploy-linux",
  "version": "0.0.1",
  "main": "application.js",
  "scripts": {
    "start": "echo 'Starting...' && pm2 start --env production && pm2 logs djaty-test-deployment && pm2 status",
    "build": "echo 'Building...' && mkdir dist && touch dist/myFiles.x",
    "test": "echo 'Testing...'"
  },
  "license": "ISC",
  "dependencies": {
    "pm2": "^3.1.2",
    "winston": "^3.0.0"
  },
  "devDependencies": {
    "@types/winston": "^2.3.9"
  }
}
